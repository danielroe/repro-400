# Reproduction

## How to Reproduce

Deploy using Vercel CLI in one of the following ways:

1. Run `vc deploy` in each directory (`nitro/`, `nuxt/`)

OR

2. Run `vc deploy --prebuilt` in the `vercel-build-api/` directory
